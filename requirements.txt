# Core Python Dependencies for Minded (AA-OS) - local-first, zero-infrastructure path.
# This file alone is enough to run AA-OS fully offline against SQLite + DuckDB,
# with AI disabled. It intentionally excludes anything that implies a hosted
# server dependency (Postgres client, Redis) or optional AI/demo layers -
# see requirements-server.txt, requirements-ai.txt, requirements-demo.txt.
fastapi>=0.115.0
uvicorn>=0.30.0
pydantic>=2.7.0
pydantic-settings>=2.2.0
email-validator>=2.0.0
sqlalchemy>=2.0.0
duckdb>=1.1.0
polars>=0.20.0
scipy>=1.11.0
scikit-learn>=1.3.0
statsmodels>=0.14.0
# pandas>=2.0.0,<3.0 -- pandas 3.x (a fresh, unpinned `pip install` resolves to
# 3.0.x today) ships breaking changes this codebase has not been verified
# against (the string-dtype migration, select_dtypes include=["object"]
# deprecation already observed firing in universal_analyst.py and
# universal_question_planner.py). requirements-lock.txt pins pandas==2.2.2;
# this range keeps a fresh install on the same major version until pandas 3.x
# compatibility is verified and the lock is bumped deliberately.
pandas>=2.0.0,<3.0
numpy>=1.24.0
pyarrow>=14.0.0
openpyxl>=3.1.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
# passlib==1.7.4's bcrypt backend probes bcrypt internals (__about__, a
# >72-byte wrap-bug detection hash) that modern bcrypt releases (4.1+,
# including the unpinned bcrypt 5.0.0 that a fresh `pip install -r
# requirements.txt` resolves today) removed/hardened against, breaking every
# bcrypt hash/verify call at import time (AttributeError: module 'bcrypt' has
# no attribute '__about__', then ValueError: password cannot be longer than
# 72 bytes). Pinning bcrypt itself closes this gap. See requirements-lock.txt.
bcrypt<4.1.0
python-multipart>=0.0.9
httpx>=0.27.0
alembic>=1.13.0
pytest>=8.0.0
pyyaml>=6.0.0

python-dotenv>=1.0.0
sqlglot==30.18.0
