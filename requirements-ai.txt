# Optional: AI augmentation layer (BYOK). The deterministic core runs and
# passes its full test suite with none of these installed and AI_ENABLED=false.
google-generativeai>=0.8.0
openai>=1.30.0
anthropic>=0.25.0
